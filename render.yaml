services:
  - type: web
    name: juego-portafolio
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app

envVars:
  - key: FLASK_CONFIG_TYPE
    value: production
